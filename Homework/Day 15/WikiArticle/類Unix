類Unix系統（英語：Unix-like；經常被稱為UN*X或*nix）指各種Unix的衍生系統，比如FreeBSD、OpenBSD、SUN的Solaris，以及各種與傳統Unix類似的系統，例如Minix、Linux、QNX等。它們雖然有的是自由軟體，有的是私有軟體，但都相當程度地繼承了原始UNIX的特性，有許多相似處，並且都在一定程度上遵守POSIX規範。

UNIX的商標權由國際開放標準組織所擁有，只有符合單一UNIX規範的UNIX系統才能使用UNIX這個名稱，否則只能稱為類UNIX（UNIX-like）。

